"use client";

// Presence layer stub. A validated presence source now exists (I3-1):
// `@anvilkit/canvas-editor/collab`'s `useCanvasPresence()`. Cursor rendering
// from that source is deferred to a later iteration — no collab UI ships yet.
export function RemoteCursors(): null {
	return null;
}
